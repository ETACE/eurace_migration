/**
 * \file  ClearingHouse_agent_header.h
 * \brief Header for agent type memory access.
 */

/** \def ID
 * \brief Direct access to id of ClearingHouse agent memory variable. */
#define ID (current_xmachine_ClearingHouse->id)
/** \def REGION_ID
 * \brief Direct access to region_id of ClearingHouse agent memory variable. */
#define REGION_ID (current_xmachine_ClearingHouse->region_id)
/** \def PARTITION_ID
 * \brief Direct access to partition_id of ClearingHouse agent memory variable. */
#define PARTITION_ID (current_xmachine_ClearingHouse->partition_id)
/** \def PENDING_ORDERS
 * \brief Direct access to pending_orders of ClearingHouse agent memory variable. */
#define PENDING_ORDERS (current_xmachine_ClearingHouse->pending_orders)
/** \def PROCESSED_ORDERS
 * \brief Direct access to processed_orders of ClearingHouse agent memory variable. */
#define PROCESSED_ORDERS (current_xmachine_ClearingHouse->processed_orders)
/** \def STOCK_INDEX
 * \brief Direct access to stock_index of ClearingHouse agent memory variable. */
#define STOCK_INDEX (current_xmachine_ClearingHouse->stock_index)
/** \def DSRATIO
 * \brief Direct access to dsratio of ClearingHouse agent memory variable. */
#define DSRATIO (current_xmachine_ClearingHouse->dsratio)
/** \def RANDOM_NUMBERS_NORMAL
 * \brief Direct access to random_numbers_normal of ClearingHouse agent memory variable. */
#define RANDOM_NUMBERS_NORMAL (current_xmachine_ClearingHouse->random_numbers_normal)
/** \def MONTHLY_COUNTER_TOTAL_DIVIDEND
 * \brief Direct access to monthly_counter_total_dividend of ClearingHouse agent memory variable. */
#define MONTHLY_COUNTER_TOTAL_DIVIDEND (current_xmachine_ClearingHouse->monthly_counter_total_dividend)
/** \def STOCK_INDEX_PRICE
 * \brief Direct access to stock_index_price of ClearingHouse agent memory variable. */
#define STOCK_INDEX_PRICE (current_xmachine_ClearingHouse->stock_index_price)
