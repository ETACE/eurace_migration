/**
 * \file  Mall_agent_header.h
 * \brief Header for agent type memory access.
 */

/** \def ID
 * \brief Direct access to id of Mall agent memory variable. */
#define ID (current_xmachine_Mall->id)
/** \def REGION_ID
 * \brief Direct access to region_id of Mall agent memory variable. */
#define REGION_ID (current_xmachine_Mall->region_id)
/** \def GOV_ID
 * \brief Direct access to gov_id of Mall agent memory variable. */
#define GOV_ID (current_xmachine_Mall->gov_id)
/** \def PARTITION_ID
 * \brief Direct access to partition_id of Mall agent memory variable. */
#define PARTITION_ID (current_xmachine_Mall->partition_id)
/** \def CURRENT_STOCK
 * \brief Direct access to current_stock of Mall agent memory variable. */
#define CURRENT_STOCK (current_xmachine_Mall->current_stock)
/** \def FIRM_REVENUES
 * \brief Direct access to firm_revenues of Mall agent memory variable. */
#define FIRM_REVENUES (current_xmachine_Mall->firm_revenues)
/** \def TOTAL_SUPPLY
 * \brief Direct access to total_supply of Mall agent memory variable. */
#define TOTAL_SUPPLY (current_xmachine_Mall->total_supply)
/** \def SALES_PER_MONTH
 * \brief Direct access to sales_per_month of Mall agent memory variable. */
#define SALES_PER_MONTH (current_xmachine_Mall->sales_per_month)
/** \def EXPORT_VOLUME_MATRIX
 * \brief Direct access to export_volume_matrix of Mall agent memory variable. */
#define EXPORT_VOLUME_MATRIX (current_xmachine_Mall->export_volume_matrix)
/** \def EXPORT_VALUE_MATRIX
 * \brief Direct access to export_value_matrix of Mall agent memory variable. */
#define EXPORT_VALUE_MATRIX (current_xmachine_Mall->export_value_matrix)
/** \def EXPORT_PREVIOUS_VALUE_MATRIX
 * \brief Direct access to export_previous_value_matrix of Mall agent memory variable. */
#define EXPORT_PREVIOUS_VALUE_MATRIX (current_xmachine_Mall->export_previous_value_matrix)
