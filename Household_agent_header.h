/**
 * \file  Household_agent_header.h
 * \brief Header for agent type memory access.
 */

/** \def ID
 * \brief Direct access to id of Household agent memory variable. */
#define ID (current_xmachine_Household->id)
/** \def REGION_ID
 * \brief Direct access to region_id of Household agent memory variable. */
#define REGION_ID (current_xmachine_Household->region_id)
/** \def NEIGHBORING_REGION_IDS
 * \brief Direct access to neighboring_region_ids of Household agent memory variable. */
#define NEIGHBORING_REGION_IDS (current_xmachine_Household->neighboring_region_ids)
/** \def GOV_ID
 * \brief Direct access to gov_id of Household agent memory variable. */
#define GOV_ID (current_xmachine_Household->gov_id)
/** \def DAY_OF_MONTH_TO_ACT
 * \brief Direct access to day_of_month_to_act of Household agent memory variable. */
#define DAY_OF_MONTH_TO_ACT (current_xmachine_Household->day_of_month_to_act)
/** \def PAYMENT_ACCOUNT
 * \brief Direct access to payment_account of Household agent memory variable. */
#define PAYMENT_ACCOUNT (current_xmachine_Household->payment_account)
/** \def BANK_ID
 * \brief Direct access to bank_id of Household agent memory variable. */
#define BANK_ID (current_xmachine_Household->bank_id)
/** \def PARTITION_ID
 * \brief Direct access to partition_id of Household agent memory variable. */
#define PARTITION_ID (current_xmachine_Household->partition_id)
/** \def NEW_AGENT
 * \brief Direct access to new_agent of Household agent memory variable. */
#define NEW_AGENT (current_xmachine_Household->new_agent)
/** \def MIGRATION_STATUS
 * \brief Direct access to migration_status of Household agent memory variable. */
#define MIGRATION_STATUS (current_xmachine_Household->migration_status)
/** \def CONSUMPTION_BUDGET
 * \brief Direct access to consumption_budget of Household agent memory variable. */
#define CONSUMPTION_BUDGET (current_xmachine_Household->consumption_budget)
/** \def CONSUMPTION_BUDGET_IN_MONTH
 * \brief Direct access to consumption_budget_in_month of Household agent memory variable. */
#define CONSUMPTION_BUDGET_IN_MONTH (current_xmachine_Household->consumption_budget_in_month)
/** \def WEEK_OF_MONTH
 * \brief Direct access to week_of_month of Household agent memory variable. */
#define WEEK_OF_MONTH (current_xmachine_Household->week_of_month)
/** \def WEEKLY_BUDGET
 * \brief Direct access to weekly_budget of Household agent memory variable. */
#define WEEKLY_BUDGET (current_xmachine_Household->weekly_budget)
/** \def RATIONED
 * \brief Direct access to rationed of Household agent memory variable. */
#define RATIONED (current_xmachine_Household->rationed)
/** \def MALL_COMPLETELY_SOLD_OUT
 * \brief Direct access to mall_completely_sold_out of Household agent memory variable. */
#define MALL_COMPLETELY_SOLD_OUT (current_xmachine_Household->mall_completely_sold_out)
/** \def ORDER_QUANTITY
 * \brief Direct access to order_quantity of Household agent memory variable. */
#define ORDER_QUANTITY (current_xmachine_Household->order_quantity)
/** \def RECEIVED_QUANTITY
 * \brief Direct access to received_quantity of Household agent memory variable. */
#define RECEIVED_QUANTITY (current_xmachine_Household->received_quantity)
/** \def DAY_OF_WEEK_TO_ACT
 * \brief Direct access to day_of_week_to_act of Household agent memory variable. */
#define DAY_OF_WEEK_TO_ACT (current_xmachine_Household->day_of_week_to_act)
/** \def DAY_OF_MONTH_RECEIVE_INCOME
 * \brief Direct access to day_of_month_receive_income of Household agent memory variable. */
#define DAY_OF_MONTH_RECEIVE_INCOME (current_xmachine_Household->day_of_month_receive_income)
/** \def CURRENT_PRODUCTIVITY_EMPLOYER
 * \brief Direct access to current_productivity_employer of Household agent memory variable. */
#define CURRENT_PRODUCTIVITY_EMPLOYER (current_xmachine_Household->current_productivity_employer)
/** \def CURRENT_MEAN_SPECIFIC_SKILLS_EMPLOYER
 * \brief Direct access to current_mean_specific_skills_employer of Household agent memory variable. */
#define CURRENT_MEAN_SPECIFIC_SKILLS_EMPLOYER (current_xmachine_Household->current_mean_specific_skills_employer)
/** \def TAX_PAYMENT
 * \brief Direct access to tax_payment of Household agent memory variable. */
#define TAX_PAYMENT (current_xmachine_Household->tax_payment)
/** \def CUM_TOTAL_DIVIDENDS
 * \brief Direct access to cum_total_dividends of Household agent memory variable. */
#define CUM_TOTAL_DIVIDENDS (current_xmachine_Household->cum_total_dividends)
/** \def TAX_RATE_HH_CAPITAL
 * \brief Direct access to tax_rate_hh_capital of Household agent memory variable. */
#define TAX_RATE_HH_CAPITAL (current_xmachine_Household->tax_rate_hh_capital)
/** \def TAX_RATE_HH_LABOUR
 * \brief Direct access to tax_rate_hh_labour of Household agent memory variable. */
#define TAX_RATE_HH_LABOUR (current_xmachine_Household->tax_rate_hh_labour)
/** \def PRICE_INDEX
 * \brief Direct access to price_index of Household agent memory variable. */
#define PRICE_INDEX (current_xmachine_Household->price_index)
/** \def PRICE_INDEX_BASE_PERIOD
 * \brief Direct access to price_index_base_period of Household agent memory variable. */
#define PRICE_INDEX_BASE_PERIOD (current_xmachine_Household->price_index_base_period)
/** \def COMMUTING_COSTS_PRICE_LEVEL_WEIGHT
 * \brief Direct access to commuting_costs_price_level_weight of Household agent memory variable. */
#define COMMUTING_COSTS_PRICE_LEVEL_WEIGHT (current_xmachine_Household->commuting_costs_price_level_weight)
/** \def EXPENDITURES
 * \brief Direct access to expenditures of Household agent memory variable. */
#define EXPENDITURES (current_xmachine_Household->expenditures)
/** \def RECEIVED_DIVIDEND
 * \brief Direct access to received_dividend of Household agent memory variable. */
#define RECEIVED_DIVIDEND (current_xmachine_Household->received_dividend)
/** \def RECEIVED_DIVIDEND_IN_CALENDAR_MONTH
 * \brief Direct access to received_dividend_in_calendar_month of Household agent memory variable. */
#define RECEIVED_DIVIDEND_IN_CALENDAR_MONTH (current_xmachine_Household->received_dividend_in_calendar_month)
/** \def RECEIVED_INTEREST_DAILY
 * \brief Direct access to received_interest_daily of Household agent memory variable. */
#define RECEIVED_INTEREST_DAILY (current_xmachine_Household->received_interest_daily)
/** \def RECEIVED_INTEREST_IN_CALENDAR_MONTH
 * \brief Direct access to received_interest_in_calendar_month of Household agent memory variable. */
#define RECEIVED_INTEREST_IN_CALENDAR_MONTH (current_xmachine_Household->received_interest_in_calendar_month)
/** \def FLAG_CONSUMPTION_SHIFTING
 * \brief Direct access to flag_consumption_shifting of Household agent memory variable. */
#define FLAG_CONSUMPTION_SHIFTING (current_xmachine_Household->flag_consumption_shifting)
/** \def EXCESS_WEEKLY_BUDGET
 * \brief Direct access to excess_weekly_budget of Household agent memory variable. */
#define EXCESS_WEEKLY_BUDGET (current_xmachine_Household->excess_weekly_budget)
/** \def WEALTH_INCOME_RATIO_ACTUAL
 * \brief Direct access to wealth_income_ratio_actual of Household agent memory variable. */
#define WEALTH_INCOME_RATIO_ACTUAL (current_xmachine_Household->wealth_income_ratio_actual)
/** \def WEALTH_INCOME_RATIO_TARGET
 * \brief Direct access to wealth_income_ratio_target of Household agent memory variable. */
#define WEALTH_INCOME_RATIO_TARGET (current_xmachine_Household->wealth_income_ratio_target)
/** \def LAST_WEEKLY_BUDGET
 * \brief Direct access to last_weekly_budget of Household agent memory variable. */
#define LAST_WEEKLY_BUDGET (current_xmachine_Household->last_weekly_budget)
/** \def RISK_FREE_RATE
 * \brief Direct access to risk_free_rate of Household agent memory variable. */
#define RISK_FREE_RATE (current_xmachine_Household->risk_free_rate)
/** \def WEALTH
 * \brief Direct access to wealth of Household agent memory variable. */
#define WEALTH (current_xmachine_Household->wealth)
/** \def BELIEFS
 * \brief Direct access to beliefs of Household agent memory variable. */
#define BELIEFS (current_xmachine_Household->beliefs)
/** \def ASSETSOWNED
 * \brief Direct access to assetsowned of Household agent memory variable. */
#define ASSETSOWNED (current_xmachine_Household->assetsowned)
/** \def LOGIT_BETA
 * \brief Direct access to logit_beta of Household agent memory variable. */
#define LOGIT_BETA (current_xmachine_Household->logit_beta)
/** \def WAGE
 * \brief Direct access to wage of Household agent memory variable. */
#define WAGE (current_xmachine_Household->wage)
/** \def WAGE_RESERVATION
 * \brief Direct access to wage_reservation of Household agent memory variable. */
#define WAGE_RESERVATION (current_xmachine_Household->wage_reservation)
/** \def GENERAL_SKILL
 * \brief Direct access to general_skill of Household agent memory variable. */
#define GENERAL_SKILL (current_xmachine_Household->general_skill)
/** \def LIST_ADAPTATION_SPEED_PER_GENERAL_SKILL_GROUP
 * \brief Direct access to list_adaptation_speed_per_general_skill_group of Household agent memory variable. */
#define LIST_ADAPTATION_SPEED_PER_GENERAL_SKILL_GROUP (current_xmachine_Household->list_adaptation_speed_per_general_skill_group)
/** \def ON_THE_JOB_SEARCH
 * \brief Direct access to on_the_job_search of Household agent memory variable. */
#define ON_THE_JOB_SEARCH (current_xmachine_Household->on_the_job_search)
/** \def SEARCH_TODAY
 * \brief Direct access to search_today of Household agent memory variable. */
#define SEARCH_TODAY (current_xmachine_Household->search_today)
/** \def LAST_LABOUR_INCOME
 * \brief Direct access to last_labour_income of Household agent memory variable. */
#define LAST_LABOUR_INCOME (current_xmachine_Household->last_labour_income)
/** \def SPECIFIC_SKILL
 * \brief Direct access to specific_skill of Household agent memory variable. */
#define SPECIFIC_SKILL (current_xmachine_Household->specific_skill)
/** \def EMPLOYEE_FIRM_ID
 * \brief Direct access to employee_firm_id of Household agent memory variable. */
#define EMPLOYEE_FIRM_ID (current_xmachine_Household->employee_firm_id)
/** \def HH_APPLICATIONS_PER_DAY
 * \brief Direct access to hh_applications_per_day of Household agent memory variable. */
#define HH_APPLICATIONS_PER_DAY (current_xmachine_Household->hh_applications_per_day)
/** \def JUST_EMPLOYED
 * \brief Direct access to just_employed of Household agent memory variable. */
#define JUST_EMPLOYED (current_xmachine_Household->just_employed)
/** \def JUST_UNEMPLOYED
 * \brief Direct access to just_unemployed of Household agent memory variable. */
#define JUST_UNEMPLOYED (current_xmachine_Household->just_unemployed)
/** \def START_UNEMPLOYED
 * \brief Direct access to start_unemployed of Household agent memory variable. */
#define START_UNEMPLOYED (current_xmachine_Household->start_unemployed)
/** \def START_EMPLOYED
 * \brief Direct access to start_employed of Household agent memory variable. */
#define START_EMPLOYED (current_xmachine_Household->start_employed)
/** \def UNEMPLOYED_DURATION
 * \brief Direct access to unemployed_duration of Household agent memory variable. */
#define UNEMPLOYED_DURATION (current_xmachine_Household->unemployed_duration)
/** \def ENTER_MATCHING
 * \brief Direct access to enter_matching of Household agent memory variable. */
#define ENTER_MATCHING (current_xmachine_Household->enter_matching)
/** \def EMPLOYER_REGION_ID
 * \brief Direct access to employer_region_id of Household agent memory variable. */
#define EMPLOYER_REGION_ID (current_xmachine_Household->employer_region_id)
/** \def EMPLOYER_IGFIRM
 * \brief Direct access to employer_igfirm of Household agent memory variable. */
#define EMPLOYER_IGFIRM (current_xmachine_Household->employer_igfirm)
/** \def DAY_OF_MONTH_RECEIVE_BENEFIT
 * \brief Direct access to day_of_month_receive_benefit of Household agent memory variable. */
#define DAY_OF_MONTH_RECEIVE_BENEFIT (current_xmachine_Household->day_of_month_receive_benefit)
/** \def UNEMPLOYMENT_PAYMENT
 * \brief Direct access to unemployment_payment of Household agent memory variable. */
#define UNEMPLOYMENT_PAYMENT (current_xmachine_Household->unemployment_payment)
/** \def REGION_WIDE_MEAN_WAGE
 * \brief Direct access to region_wide_mean_wage of Household agent memory variable. */
#define REGION_WIDE_MEAN_WAGE (current_xmachine_Household->region_wide_mean_wage)
/** \def BASIC_SECURITY_BENEFITS
 * \brief Direct access to basic_security_benefits of Household agent memory variable. */
#define BASIC_SECURITY_BENEFITS (current_xmachine_Household->basic_security_benefits)
/** \def LAST_NET_INCOME
 * \brief Direct access to last_net_income of Household agent memory variable. */
#define LAST_NET_INCOME (current_xmachine_Household->last_net_income)
/** \def MEAN_NET_INCOME
 * \brief Direct access to mean_net_income of Household agent memory variable. */
#define MEAN_NET_INCOME (current_xmachine_Household->mean_net_income)
/** \def HOUSEHOLD_BALANCE_SHEET_CALENDAR
 * \brief Direct access to household_balance_sheet_calendar of Household agent memory variable. */
#define HOUSEHOLD_BALANCE_SHEET_CALENDAR (current_xmachine_Household->household_balance_sheet_calendar)
/** \def HOUSEHOLD_STOCKS_CALENDAR
 * \brief Direct access to household_stocks_calendar of Household agent memory variable. */
#define HOUSEHOLD_STOCKS_CALENDAR (current_xmachine_Household->household_stocks_calendar)
/** \def HOUSEHOLD_OUTFLOWS_CALENDAR
 * \brief Direct access to household_outflows_calendar of Household agent memory variable. */
#define HOUSEHOLD_OUTFLOWS_CALENDAR (current_xmachine_Household->household_outflows_calendar)
/** \def HOUSEHOLD_INFLOWS_CALENDAR
 * \brief Direct access to household_inflows_calendar of Household agent memory variable. */
#define HOUSEHOLD_INFLOWS_CALENDAR (current_xmachine_Household->household_inflows_calendar)
/** \def UNEMPLOYMENT_BENEFIT_PCT
 * \brief Direct access to unemployment_benefit_pct of Household agent memory variable. */
#define UNEMPLOYMENT_BENEFIT_PCT (current_xmachine_Household->unemployment_benefit_pct)
/** \def MINIMUM_WAGE
 * \brief Direct access to minimum_wage of Household agent memory variable. */
#define MINIMUM_WAGE (current_xmachine_Household->minimum_wage)
/** \def TRANSFER_PAYMENT
 * \brief Direct access to transfer_payment of Household agent memory variable. */
#define TRANSFER_PAYMENT (current_xmachine_Household->transfer_payment)
/** \def SUBSIDY_PCT
 * \brief Direct access to subsidy_pct of Household agent memory variable. */
#define SUBSIDY_PCT (current_xmachine_Household->subsidy_pct)
/** \def MONTHLY_INTEREST_INCOME
 * \brief Direct access to monthly_interest_income of Household agent memory variable. */
#define MONTHLY_INTEREST_INCOME (current_xmachine_Household->monthly_interest_income)
/** \def TOTAL_INCOME
 * \brief Direct access to total_income of Household agent memory variable. */
#define TOTAL_INCOME (current_xmachine_Household->total_income)
/** \def TOTAL_EXPENSES
 * \brief Direct access to total_expenses of Household agent memory variable. */
#define TOTAL_EXPENSES (current_xmachine_Household->total_expenses)
/** \def TOTAL_ASSETS
 * \brief Direct access to total_assets of Household agent memory variable. */
#define TOTAL_ASSETS (current_xmachine_Household->total_assets)
/** \def TOTAL_LIABILITIES
 * \brief Direct access to total_liabilities of Household agent memory variable. */
#define TOTAL_LIABILITIES (current_xmachine_Household->total_liabilities)
/** \def MONTHLY_CONSUMPTION_EXPENDITURE
 * \brief Direct access to monthly_consumption_expenditure of Household agent memory variable. */
#define MONTHLY_CONSUMPTION_EXPENDITURE (current_xmachine_Household->monthly_consumption_expenditure)
/** \def MONTHLY_BOND_INTEREST_INCOME
 * \brief Direct access to monthly_bond_interest_income of Household agent memory variable. */
#define MONTHLY_BOND_INTEREST_INCOME (current_xmachine_Household->monthly_bond_interest_income)
/** \def SUBSIDY_PAYMENT
 * \brief Direct access to subsidy_payment of Household agent memory variable. */
#define SUBSIDY_PAYMENT (current_xmachine_Household->subsidy_payment)
/** \def REGION_MEAN_WAGE
 * \brief Direct access to region_mean_wage of Household agent memory variable. */
#define REGION_MEAN_WAGE (current_xmachine_Household->region_mean_wage)
/** \def HUMAN_CAPITAL_POLICY_FLAG
 * \brief Direct access to human_capital_policy_flag of Household agent memory variable. */
#define HUMAN_CAPITAL_POLICY_FLAG (current_xmachine_Household->human_capital_policy_flag)
/** \def QUESTIONNAIRE_ATTENDING_PROPABILITY
 * \brief Direct access to questionnaire_attending_propability of Household agent memory variable. */
#define QUESTIONNAIRE_ATTENDING_PROPABILITY (current_xmachine_Household->questionnaire_attending_propability)
/** \def WILLINGNESS_TO_ATTEND
 * \brief Direct access to willingness_to_attend of Household agent memory variable. */
#define WILLINGNESS_TO_ATTEND (current_xmachine_Household->willingness_to_attend)
/** \def INTERVIEWER_ID
 * \brief Direct access to interviewer_id of Household agent memory variable. */
#define INTERVIEWER_ID (current_xmachine_Household->interviewer_id)
/** \def RANDOM_NO_TEST
 * \brief Direct access to random_no_test of Household agent memory variable. */
#define RANDOM_NO_TEST (current_xmachine_Household->random_no_test)
/** \def QUESTIONNAIRE
 * \brief Direct access to questionnaire of Household agent memory variable. */
#define QUESTIONNAIRE (current_xmachine_Household->questionnaire)
/** \def CURRENT_PRICE_QUALITY_LIST
 * \brief Direct access to current_price_quality_list of Household agent memory variable. */
#define CURRENT_PRICE_QUALITY_LIST (current_xmachine_Household->current_price_quality_list)
