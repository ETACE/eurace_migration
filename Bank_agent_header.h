/**
 * \file  Bank_agent_header.h
 * \brief Header for agent type memory access.
 */

/** \def ID
 * \brief Direct access to id of Bank agent memory variable. */
#define ID (current_xmachine_Bank->id)
/** \def REGION_ID
 * \brief Direct access to region_id of Bank agent memory variable. */
#define REGION_ID (current_xmachine_Bank->region_id)
/** \def GOV_ID
 * \brief Direct access to gov_id of Bank agent memory variable. */
#define GOV_ID (current_xmachine_Bank->gov_id)
/** \def PARTITION_ID
 * \brief Direct access to partition_id of Bank agent memory variable. */
#define PARTITION_ID (current_xmachine_Bank->partition_id)
/** \def ACTIVE
 * \brief Direct access to active of Bank agent memory variable. */
#define ACTIVE (current_xmachine_Bank->active)
/** \def INACTIVE
 * \brief Direct access to inactive of Bank agent memory variable. */
#define INACTIVE (current_xmachine_Bank->inactive)
/** \def CASH
 * \brief Direct access to cash of Bank agent memory variable. */
#define CASH (current_xmachine_Bank->cash)
/** \def TOTAL_CREDIT
 * \brief Direct access to total_credit of Bank agent memory variable. */
#define TOTAL_CREDIT (current_xmachine_Bank->total_credit)
/** \def EQUITY
 * \brief Direct access to equity of Bank agent memory variable. */
#define EQUITY (current_xmachine_Bank->equity)
/** \def ECB_DEBT
 * \brief Direct access to ecb_debt of Bank agent memory variable. */
#define ECB_DEBT (current_xmachine_Bank->ecb_debt)
/** \def VALUE_AT_RISK
 * \brief Direct access to value_at_risk of Bank agent memory variable. */
#define VALUE_AT_RISK (current_xmachine_Bank->value_at_risk)
/** \def DEPOSIT_INTEREST_RATE
 * \brief Direct access to deposit_interest_rate of Bank agent memory variable. */
#define DEPOSIT_INTEREST_RATE (current_xmachine_Bank->deposit_interest_rate)
/** \def BANK_GAMMA
 * \brief Direct access to bank_gamma of Bank agent memory variable. */
#define BANK_GAMMA (current_xmachine_Bank->bank_gamma)
/** \def PROFITS
 * \brief Direct access to profits of Bank agent memory variable. */
#define PROFITS (current_xmachine_Bank->profits)
/** \def BANK_DIVIDEND_RATE
 * \brief Direct access to bank_dividend_rate of Bank agent memory variable. */
#define BANK_DIVIDEND_RATE (current_xmachine_Bank->bank_dividend_rate)
/** \def TAX_RATE_CORPORATE
 * \brief Direct access to tax_rate_corporate of Bank agent memory variable. */
#define TAX_RATE_CORPORATE (current_xmachine_Bank->tax_rate_corporate)
/** \def CURRENT_SHARES_OUTSTANDING
 * \brief Direct access to current_shares_outstanding of Bank agent memory variable. */
#define CURRENT_SHARES_OUTSTANDING (current_xmachine_Bank->current_shares_outstanding)
/** \def DEBT_PERIOD
 * \brief Direct access to debt_period of Bank agent memory variable. */
#define DEBT_PERIOD (current_xmachine_Bank->debt_period)
/** \def LOAN_REQUEST_MESSAGE_FOUND
 * \brief Direct access to loan_request_message_found of Bank agent memory variable. */
#define LOAN_REQUEST_MESSAGE_FOUND (current_xmachine_Bank->loan_request_message_found)
/** \def DAY_OF_MONTH_TO_ACT
 * \brief Direct access to day_of_month_to_act of Bank agent memory variable. */
#define DAY_OF_MONTH_TO_ACT (current_xmachine_Bank->day_of_month_to_act)
/** \def DEPOSITS
 * \brief Direct access to deposits of Bank agent memory variable. */
#define DEPOSITS (current_xmachine_Bank->deposits)
/** \def FIRM_LOAN_INSTALLMENTS
 * \brief Direct access to firm_loan_installments of Bank agent memory variable. */
#define FIRM_LOAN_INSTALLMENTS (current_xmachine_Bank->firm_loan_installments)
/** \def FIRM_LOAN_INSTALLMENTS_IN_CALENDAR_MONTH
 * \brief Direct access to firm_loan_installments_in_calendar_month of Bank agent memory variable. */
#define FIRM_LOAN_INSTALLMENTS_IN_CALENDAR_MONTH (current_xmachine_Bank->firm_loan_installments_in_calendar_month)
/** \def FIRM_INTEREST_PAYMENTS
 * \brief Direct access to firm_interest_payments of Bank agent memory variable. */
#define FIRM_INTEREST_PAYMENTS (current_xmachine_Bank->firm_interest_payments)
/** \def FIRM_INTEREST_PAYMENTS_IN_CALENDAR_MONTH
 * \brief Direct access to firm_interest_payments_in_calendar_month of Bank agent memory variable. */
#define FIRM_INTEREST_PAYMENTS_IN_CALENDAR_MONTH (current_xmachine_Bank->firm_interest_payments_in_calendar_month)
/** \def FIRM_LOAN_ISSUES
 * \brief Direct access to firm_loan_issues of Bank agent memory variable. */
#define FIRM_LOAN_ISSUES (current_xmachine_Bank->firm_loan_issues)
/** \def ECB_INTEREST_PAYMENT
 * \brief Direct access to ecb_interest_payment of Bank agent memory variable. */
#define ECB_INTEREST_PAYMENT (current_xmachine_Bank->ecb_interest_payment)
/** \def TOTAL_DIVIDEND_PAYMENT
 * \brief Direct access to total_dividend_payment of Bank agent memory variable. */
#define TOTAL_DIVIDEND_PAYMENT (current_xmachine_Bank->total_dividend_payment)
/** \def TOTAL_DIVIDENDS_IN_CALENDAR_MONTH
 * \brief Direct access to total_dividends_in_calendar_month of Bank agent memory variable. */
#define TOTAL_DIVIDENDS_IN_CALENDAR_MONTH (current_xmachine_Bank->total_dividends_in_calendar_month)
/** \def TAXES
 * \brief Direct access to taxes of Bank agent memory variable. */
#define TAXES (current_xmachine_Bank->taxes)
/** \def DIVIDEND_PER_SHARE
 * \brief Direct access to dividend_per_share of Bank agent memory variable. */
#define DIVIDEND_PER_SHARE (current_xmachine_Bank->dividend_per_share)
/** \def CURRENT_SHARE_PRICE
 * \brief Direct access to current_share_price of Bank agent memory variable. */
#define CURRENT_SHARE_PRICE (current_xmachine_Bank->current_share_price)
/** \def LEVERAGE_RATIO
 * \brief Direct access to leverage_ratio of Bank agent memory variable. */
#define LEVERAGE_RATIO (current_xmachine_Bank->leverage_ratio)
/** \def CASH_DEPOSIT_RATIO
 * \brief Direct access to cash_deposit_ratio of Bank agent memory variable. */
#define CASH_DEPOSIT_RATIO (current_xmachine_Bank->cash_deposit_ratio)
/** \def EQUITY_ASSET_RATIO
 * \brief Direct access to equity_asset_ratio of Bank agent memory variable. */
#define EQUITY_ASSET_RATIO (current_xmachine_Bank->equity_asset_ratio)
/** \def MEAN_INTEREST_RATE
 * \brief Direct access to mean_interest_rate of Bank agent memory variable. */
#define MEAN_INTEREST_RATE (current_xmachine_Bank->mean_interest_rate)
/** \def SUM_NEW_INTEREST_RATES_IN_CALENDAR_MONTH
 * \brief Direct access to sum_new_interest_rates_in_calendar_month of Bank agent memory variable. */
#define SUM_NEW_INTEREST_RATES_IN_CALENDAR_MONTH (current_xmachine_Bank->sum_new_interest_rates_in_calendar_month)
/** \def NO_NEW_LOANS_IN_CALENDAR_MONTH
 * \brief Direct access to no_new_loans_in_calendar_month of Bank agent memory variable. */
#define NO_NEW_LOANS_IN_CALENDAR_MONTH (current_xmachine_Bank->no_new_loans_in_calendar_month)
/** \def PAID_INTEREST_DAILY
 * \brief Direct access to paid_interest_daily of Bank agent memory variable. */
#define PAID_INTEREST_DAILY (current_xmachine_Bank->paid_interest_daily)
/** \def PAID_INTEREST_IN_CALENDAR_MONTH
 * \brief Direct access to paid_interest_in_calendar_month of Bank agent memory variable. */
#define PAID_INTEREST_IN_CALENDAR_MONTH (current_xmachine_Bank->paid_interest_in_calendar_month)
/** \def EXCESS_VAR
 * \brief Direct access to excess_var of Bank agent memory variable. */
#define EXCESS_VAR (current_xmachine_Bank->excess_var)
/** \def EXCESS_LIQUIDITY
 * \brief Direct access to excess_liquidity of Bank agent memory variable. */
#define EXCESS_LIQUIDITY (current_xmachine_Bank->excess_liquidity)
/** \def TOTAL_BAD_DEBT_CALENDAR_MONTH
 * \brief Direct access to total_bad_debt_calendar_month of Bank agent memory variable. */
#define TOTAL_BAD_DEBT_CALENDAR_MONTH (current_xmachine_Bank->total_bad_debt_calendar_month)
/** \def BANK_BALANCE_SHEET_CALENDAR
 * \brief Direct access to bank_balance_sheet_calendar of Bank agent memory variable. */
#define BANK_BALANCE_SHEET_CALENDAR (current_xmachine_Bank->bank_balance_sheet_calendar)
/** \def BANK_STOCKS_CALENDAR
 * \brief Direct access to bank_stocks_calendar of Bank agent memory variable. */
#define BANK_STOCKS_CALENDAR (current_xmachine_Bank->bank_stocks_calendar)
/** \def BANK_OUTFLOWS_CALENDAR
 * \brief Direct access to bank_outflows_calendar of Bank agent memory variable. */
#define BANK_OUTFLOWS_CALENDAR (current_xmachine_Bank->bank_outflows_calendar)
/** \def BANK_INFLOWS_CALENDAR
 * \brief Direct access to bank_inflows_calendar of Bank agent memory variable. */
#define BANK_INFLOWS_CALENDAR (current_xmachine_Bank->bank_inflows_calendar)
/** \def TOTAL_INCOME
 * \brief Direct access to total_income of Bank agent memory variable. */
#define TOTAL_INCOME (current_xmachine_Bank->total_income)
/** \def TOTAL_EXPENSES
 * \brief Direct access to total_expenses of Bank agent memory variable. */
#define TOTAL_EXPENSES (current_xmachine_Bank->total_expenses)
/** \def TOTAL_ASSETS
 * \brief Direct access to total_assets of Bank agent memory variable. */
#define TOTAL_ASSETS (current_xmachine_Bank->total_assets)
/** \def TOTAL_LIABILITIES
 * \brief Direct access to total_liabilities of Bank agent memory variable. */
#define TOTAL_LIABILITIES (current_xmachine_Bank->total_liabilities)
